//I forgot my SSH again so no test for me.
#include <stdio.h>
#include <stdlib.h>
#include "mUtil.h"




int main(void){
  printf("\n");
  
  // void* ptr = malloc(sizeof(int) * 10);
  // printf("%p, %lu\n", ptr, sizeof(ptr[0]));
  // ptr = (int*)ptr;
  // printf("%p, %lu\n", ptr, sizeof(ptr[0]));

  printf("\n");
  return 0;
}